machine:
  services:
    - docker

dependencies:
  override:
    - sudo -H pip install docker-compose

test:
  override:
    - docker-compose run web python manage.py test

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY